# Normalize line endings so Windows checkouts don't produce whole-file diffs.
* text=auto eol=lf

# Windows-native scripts keep CRLF.
*.ps1 text eol=crlf

# Never munge binaries or subtitle payloads.
*.mp4 binary
*.m4a binary
*.jpg binary
*.ass -text
*.json3 -text
