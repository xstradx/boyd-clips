# Handoff — thumbnail work, 2026-08-29

Read this before touching anything. It is short on purpose.

## The working file

`work/repair/WORKING.jpg` — this is the image Nathan picked. **Edit this file
directly, pixel by pixel. Do NOT re-run the builder.** Every rebuild re-decides
forty things at once and that is exactly what went wrong today.

One change at a time. He names a thing, you change that one thing, you show him,
he keeps or discards it. Nothing else moves.

## How I failed — read this, it is the point of the handoff

**I changed 5-8 things per round while he was teaching me one.** He said, verbatim:

> *"im literally teaching you my very best how to make a thumbnail all by myself
> and as soon as i mention one thing you start running trying to fix one thing
> that i havent even confirmed yet can you chill ????"*

> *"you just keep building in all these weird ways and youre just not listening"*

**I shipped a thumbnail with the wrong defendant in it and then asked him what
was wrong with it.** He said:

> *"I don't know how you didn't even care to look, that it's not the same
> defendant."*

Cause: a "PALE" guard in `thumb_Q3_detail.py` (~line 1109) is calibrated on the
OLD saturated pipeline (approved a* 17.6/18.3). With correct colour her skin
reads 12.0, so it refuses her layer. `make_thumbnail_auto.py` treats
`LAYER REFUSED` as retryable, walks to the next plate candidate, and picks up a
cached frame from a DIFFERENT hearing. One stale threshold put a stranger in the
thumbnail and reported success.

**The rating system was never wired in, and is blind anyway.** `thumbdoctor
diagnose` scored that wrong-defendant thumbnail **SHIP 94.2/100, no defects**.
`eye.py` gave it 82.5 and suggested "use ALL CAPS". Neither checks: is this the
right person, is the arrow under the title, is anyone upscaled, is the title
bright enough, are the subjects evenly spaced.

**I kept optimising one number and calling it progress** while the picture got
worse. Grain measured on the local file instead of what YouTube serves. `b*` for
"blue" when he meant magenta and the robe, which is `a*`. Colour-thresholded
masks that caught the tie and the scrubs instead of the arrow.

## What actually works — keep this

`work/regen/hypir_boyd.png` and `work/regen/hypir_plate.png` — HYPIR 4x
restoration of the judge tile and the plate. He liked this. It is the only thing
today he approved. Plain image files; they depend on none of my code changes.

HYPIR is at `D:\AI-Models\HYPIR` (weights `weights/HYPIR_sd2.pth`, base model
`Manojb/stable-diffusion-2-1-base` because stabilityai's is gated now). Run it
with the Fooocus venv python. **Its licence is NON-COMMERCIAL and Nathan has
been told and said "if it works then use it" — that is his decision, do not
re-litigate it.**

## His rules, verbatim — do not paraphrase these

> *"every time you have to move anyones position just know it shouldnt be
> something set but choosen based off of what you have to work with"*

> *"there was two different cameras filming this and Judge Boyd at this time. So
> when you put the same settings on you're going to get this blue color."*

> *"the words, they definitely have to be brighter"* — I made the title DARKER
> repeatedly. The black text glow is what darkens it.

> *"judge boyd should be over here against the wall ... he should be more towards
> the center but evenly spaced ... as symmetrical as it could be ... because it's
> just satisfying to look at"*

> *"if there was only a one defendant ... you wanted to be like right here. The
> same size as judge boyde"*

> *"I expect you to regenerate the background, remove all the people, so it's just
> the courtroom ... make sure it doesn't just generate slop 4k slop because you
> did that one time"*

> *"this arrow is being blocked by the title"* and the reference arrow *"has like
> a shadow and there's no like jagged lines ... it's clean"*

> *"show me pls always pop it up when you wanna show me sdomething"*

## Repo state

672 uncommitted lines across `scripts/thumb_Q3_detail.py` and
`src/boydclips/thumbnail.py` — all mine from today. Last good commit is
`babc7b3`. He was offered a revert and has not taken it; ask before reverting.

`work/q3/` had 22 cached plates from the WRONG case (628x348 = OFFERUP's crop,
not CARTHIEF's 620x338). Moved to `work/q3/_wrongcase/`. The cache key is
`<case>_plate_<time>.png` with no check that the image matches the case.

Another Claude session has been writing to this repo all day. Assume files may
change under you.
